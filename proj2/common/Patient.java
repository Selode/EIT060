package common;

public class Patient extends Person {
	
	public Patient(String personalIdentifier, String name, SecurityLevel level, String password) {
		super(personalIdentifier, name, level, password);
	}
}